{
  "name": "soppa",
  "version": "1.0.0",
  "private": true,
  "description": "Soppa — catering-varastonhallinta. Juuressa vain kehitysskriptit; varsinaiset paketit ovat backend/ ja frontend/ -kansioissa.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  }
}
